# Sanity CMS — copy this file to .env and fill in your values
PUBLIC_SANITY_PROJECT_ID=bf0ainjd
PUBLIC_SANITY_DATASET=production

# Optional: Sanity API token with write access (needed for mutations / drafts preview)
# SANITY_API_TOKEN=
